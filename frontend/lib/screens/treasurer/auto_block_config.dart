import 'package:flutter/material.dart';

class AutoBlockConfigPage extends StatelessWidget {
  const AutoBlockConfigPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Auto Block Configuration")),
      body: const Center(child: Text("UI-406 Placeholder")),
    );
  }
}